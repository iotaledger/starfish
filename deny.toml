# This section is considered when running `cargo deny check advisories`
# More documentation for the advisories section can be found here:
# https://embarkstudios.github.io/cargo-deny/checks/advisories/cfg.html
[advisories]
version = 2
ignore = [
  # bincode 1.3.3 is unmaintained but works; no drop-in replacement
  "RUSTSEC-2025-0141",
  # proc-macro-error is unmaintained, transitive via tabled_derive
  "RUSTSEC-2024-0370",
  # protobuf 2.x recursion crash, transitive via prometheus 0.13 (can't upgrade)
  "RUSTSEC-2024-0437",
]

# This section is considered when running `cargo deny check licenses`
# More documentation for the licenses section can be found here:
# https://embarkstudios.github.io/cargo-deny/checks/licenses/cfg.html
[licenses]
version = 2
# List of explicitly allowed licenses
# See https://spdx.org/licenses/ for list of possible licenses
# [possible values: any SPDX 3.11 short identifier (+ optional exception)].
allow = [
  "0BSD",
  "Apache-2.0",
  "BSD-2-Clause",
  "BSD-3-Clause",
  "CC0-1.0",
  "ISC",
  "LicenseRef-ring",
  "MIT",
  "MPL-2.0",
  "Unicode-3.0",
  "Unicode-DFS-2016",
]
# The confidence threshold for detecting a license from license text.
# The higher the value, the more closely the license text must be to the
# canonical license text of a valid SPDX license file.
# [possible values: any between 0.0 and 1.0].
confidence-threshold = 0.8

[[licenses.clarify]]
name = "ring"
expression = "LicenseRef-ring"
license-files = [{ path = "LICENSE", hash = 0xbd0eed23 }]

[licenses.private]
# If true, ignores workspace crates that aren't published, or are only
# published to private registries.
# To see how to mark a crate as unpublished (to the official registry),
# visit https://doc.rust-lang.org/cargo/reference/manifest.html#the-publish-field.
ignore = true

# This section is considered when running `cargo deny check bans`.
# More documentation about the 'bans' section can be found here:
# https://embarkstudios.github.io/cargo-deny/checks/bans/cfg.html
[bans]
# Lint level for when multiple versions of the same crate are detected
multiple-versions = "deny"
skip = [
  { name = "itertools", version = "<=12.1" },
  # transitive bindgen duplicates from librocksdb-sys, zstd-sys build deps
  { name = "bindgen", version = "<=0.71" },
  # crossterm 0.27 uses old mio 0.8
  { name = "mio", version = "0.8" },
  # tabled_derive uses heck 0.4, clap_derive uses 0.5
  { name = "heck", version = "0.4" },
  # bindgen 0.69 (from rocksdb) uses rustc-hash 1.x
  { name = "rustc-hash", version = "1" },
]
skip-tree = [
  # aws-smithy-http-client depends on both hyper 0.14 and 1.x,
  # pulling old http/http-body/h2/rustls/tokio-rustls/socket2/core-foundation stacks
  { name = "aws-smithy-http-client" },
  # ed25519-consensus uses old sha2/digest/block-buffer chain
  { name = "ed25519-consensus", version = "2" },
  # windows-sys 0.48/0.52 duplicates from various transitive deps
  { name = "windows-sys", version = "<=0.52" },
]

# This section is considered when running `cargo deny check sources`.
# More documentation about the 'sources' section can be found here:
# https://embarkstudios.github.io/cargo-deny/checks/sources/cfg.html
[sources]
# Lint level for what to happen when a crate from a crate registry that is not
# in the allow list is encountered
unknown-registry = "deny"
# Lint level for what to happen when a crate from a git repository that is not
# in the allow list is encountered
unknown-git = "deny"
allow-git = [
  "https://github.com/asonnino/prometheus-parser",
]

[sources.allow-org]
